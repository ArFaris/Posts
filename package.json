{
    "name": "react_project",
    "private": true,
    "version": "0.0.0",
    "workspaces": [
        "backend",
        "webapp"
    ],
    "scripts": {
        "prepare": "husky",
        "b": "npm run --workspace=@react_project/backend",
        "w": "npm run --workspace=@react_project/webapp",
        "dev": "concurrently -n BACKEND,FRONTEND -c bgBlue.bold,bgGreen.bold \"npm run b dev\" \"npm run w dev\"",
        "types": "concurrently -n BACKEND,FRONTEND -c bgBlue.bold,bgGreen.bold \"npm run b types\" \"npm run w types\"",
        "lint": "concurrently -n BACKEND,FRONTEND -c bgBlue.bold,bgGreen.bold \"npm run b lint\" \"npm run w lint\"",
        "prettify": "concurrently -n BACKEND,FRONTEND -c bgBlue.bold,bgGreen.bold \"npm run b prettify\" \"npm run w prettify\""
    },
    "devDependencies": {
        "concurrently": "^9.2.0",
        "eslint": "^8.56.0",
        "eslint-config-prettier": "^8.8.0",
        "eslint-config-react-app": "^7.0.1",
        "eslint-config-standard-with-typescript": "^23.0.0",
        "husky": "^9.1.7",
        "lint-staged": "^16.1.5",
        "prettier": "^3.6.2",
        "rimraf": "^6.0.1",
        "ts-node": "^10.9.2",
        "ts-node-dev": "^2.0.0",
        "typescript": "^5.9.2"
    }
}
